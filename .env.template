# DecoPilot 环境变量配置模板
# 复制此文件为 .env 并填入实际值

# ============ 运行环境 ============
# development / production
ENV=development

# 是否开启调试模式
DEBUG=true

# ============ API 密钥 ============
# 通义千问 API Key (必填)
# 获取地址: https://dashscope.console.aliyun.com/
DASHSCOPE_API_KEY=your_dashscope_api_key_here

# ============ 安全配置 ============
# CORS 允许的源，多个用逗号分隔
# 生产环境建议设置为具体域名
CORS_ORIGINS=*

# 可信主机，多个用逗号分隔
TRUSTED_HOSTS=*

# ============ 数据库配置 ============
# 记忆系统是否持久化
MEMORY_PERSIST=true

# 记忆系统后端类型: memory / file / sqlite / redis
# 推荐使用 sqlite，支持并发访问和持久化
MEMORY_BACKEND=sqlite

# SQLite 数据库路径（相对于项目根目录）
SQLITE_DB_PATH=data/memory/memory.db

# ============ 服务配置 ============
# API 服务端口
API_PORT=8000

# 前端服务端口
FRONTEND_PORT=3000

# ============ 可选：Redis 配置 ============
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=

# ============ 可选：日志配置 ============
# 日志级别: DEBUG / INFO / WARNING / ERROR
LOG_LEVEL=INFO

# 日志格式: json / text
LOG_FORMAT=json
